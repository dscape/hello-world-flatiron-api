{
  "name": "hello-world-api-flatiron",
  "subdomain": "my-changed-subdomain",
  "description": "Sample API in Flatironjs",
  "version": "0.0.3",
  "dependencies": { "flatiron": "*", "union": "*" },
  "devDependencies": { "specify": "0.1.x" },
  "scripts": {
    "test": "ls -tr test/*.js | xargs -I{} node {}",
    "start": "node index.js"
  },
  "engines": {
    "node": "0.8.x"
  }
}
